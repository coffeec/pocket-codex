diff --git a/src/components/command-palette/CommandPalette.tsx b/src/components/command-palette/CommandPalette.tsx
--- a/src/components/command-palette/CommandPalette.tsx
+++ b/src/components/command-palette/CommandPalette.tsx
@@ -263,7 +263,7 @@
 
             {showActions && (
               <CommandGroup heading="Settings">
-                {SETTINGS_MAIN_TABS.map(({ id, label, keywords, icon: Icon }) => (
+                {SETTINGS_MAIN_TABS.filter(({ id }) => !(POCKETCODEX_MODE && id === 'browser')).map(({ id, label, keywords, icon: Icon }) => (
                   <CommandItem
                     key={id}
                     value={`Settings ${label} ${keywords}`}
diff --git a/src/components/main-content/view/MainContent.tsx b/src/components/main-content/view/MainContent.tsx
--- a/src/components/main-content/view/MainContent.tsx
+++ b/src/components/main-content/view/MainContent.tsx
@@ -16,6 +16,7 @@ import { authenticatedFetch } from '../../../utils/api';
 import { useEditorSidebar } from '../../code-editor/hooks/useEditorSidebar';
 import EditorSidebar from '../../code-editor/view/EditorSidebar';
 import type { Project } from '../../../types/app';
+import { POCKETCODEX_MODE } from '../../../constants/config';
 import { TaskMasterPanel } from '../../task-master';
 
 import MainContentHeader from './subcomponents/MainContentHeader';
@@ -61,7 +62,7 @@ function MainContent({
   const [browserUseEnabled, setBrowserUseEnabled] = useState(false);
 
   const shouldShowTasksTab = Boolean(tasksEnabled && isTaskMasterInstalled);
-  const shouldShowBrowserTab = browserUseEnabled;
+  const shouldShowBrowserTab = !POCKETCODEX_MODE && browserUseEnabled;
 
   const {
     editingFile,
@@ -110,6 +111,7 @@ function MainContent({
   }, []);
 
   useEffect(() => {
+    if (POCKETCODEX_MODE) return undefined;
     void loadBrowserUseSettings();
     window.addEventListener('browserUseSettingsChanged', loadBrowserUseSettings);
     return () => window.removeEventListener('browserUseSettingsChanged', loadBrowserUseSettings);
diff --git a/src/components/settings/hooks/useSettingsController.ts b/src/components/settings/hooks/useSettingsController.ts
--- a/src/components/settings/hooks/useSettingsController.ts
+++ b/src/components/settings/hooks/useSettingsController.ts
@@ -4,6 +4,7 @@ import { useTheme } from '../../../contexts/ThemeContext';
 import { authenticatedFetch } from '../../../utils/api';
 import { setNotificationSoundEnabled } from '../../../utils/notificationSound';
 import { useProviderAuthStatus } from '../../provider-auth/hooks/useProviderAuthStatus';
+import { POCKETCODEX_MODE } from '../../../constants/config';
 import {
   DEFAULT_CODE_EDITOR_SETTINGS,
   DEFAULT_CURSOR_PERMISSIONS,
@@ -61,7 +61,11 @@ const normalizeMainTab = (tab: string): SettingsMainTab => {
     return 'agents';
   }
 
-  return KNOWN_MAIN_TABS.includes(tab as SettingsMainTab) ? (tab as SettingsMainTab) : 'agents';
+  if (!KNOWN_MAIN_TABS.includes(tab as SettingsMainTab)) {
+    return 'agents';
+  }
+
+  return POCKETCODEX_MODE && tab === 'browser' ? 'agents' : tab as SettingsMainTab;
 };
 
 const parseJson = <T>(value: string | null, fallback: T): T => {
diff --git a/src/components/settings/view/SettingsSidebar.tsx b/src/components/settings/view/SettingsSidebar.tsx
--- a/src/components/settings/view/SettingsSidebar.tsx
+++ b/src/components/settings/view/SettingsSidebar.tsx
@@ -1,6 +1,7 @@
 import { Bell, Bot, GitBranch, Info, Key, ListChecks, Mic, MonitorPlay, Palette, Puzzle } from 'lucide-react';
 import { useTranslation } from 'react-i18next';
 
+import { POCKETCODEX_MODE } from '../../../constants/config';
 import { cn } from '../../../lib/utils';
 import { PillBar, Pill } from '../../../shared/view/ui';
 import type { SettingsMainTab } from '../types/types';
@@ -29,6 +30,10 @@ const NAV_ITEMS: NavItem[] = [
   { id: 'about', labelKey: 'mainTabs.about', icon: Info },
 ];
 
+const VISIBLE_NAV_ITEMS = POCKETCODEX_MODE
+  ? NAV_ITEMS.filter((item) => item.id !== 'browser')
+  : NAV_ITEMS;
+
 export default function SettingsSidebar({ activeTab, onChange }: SettingsSidebarProps) {
   const { t } = useTranslation('settings');
 
@@ -37,7 +42,7 @@ export default function SettingsSidebar({ activeTab, onChange }: SettingsSidebarProps {
       {/* Desktop sidebar */}
       <aside className="hidden w-56 flex-shrink-0 border-r border-border bg-muted/30 md:flex md:flex-col">
         <nav className="flex flex-col gap-1 p-3">
-          {NAV_ITEMS.map((item) => {
+          {VISIBLE_NAV_ITEMS.map((item) => {
             const Icon = item.icon;
             const isActive = activeTab === item.id;
 
@@ -63,5 +68,5 @@ export default function SettingsSidebar({ activeTab, onChange }: SettingsSidebarProps {
       {/* Mobile horizontal nav — pill bar */}
       <div className="flex-shrink-0 border-b border-border px-3 py-2 md:hidden">
         <PillBar className="scrollbar-hide w-full overflow-x-auto">
-          {NAV_ITEMS.map((item) => {
+          {VISIBLE_NAV_ITEMS.map((item) => {
             const Icon = item.icon;
