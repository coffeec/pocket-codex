diff --git a/index.html b/index.html
index 8fdd8ca..2cda902 100644
--- a/index.html
+++ b/index.html
@@ -5,7 +5,7 @@
     <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
     <link rel="icon" type="image/png" href="/favicon.png" />
     <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, user-scalable=no, viewport-fit=cover, interactive-widget=resizes-content" />
-    <title>PocketCodex Agent</title>
+    <title>PocketCodex 代码代理</title>
 
     <!-- PWA Manifest -->
     <link rel="manifest" href="/manifest.json" crossorigin="use-credentials"  />
diff --git a/public/manifest.json b/public/manifest.json
index fd4113f..62ae96c 100644
--- a/public/manifest.json
+++ b/public/manifest.json
@@ -1,11 +1,11 @@
 {
-  "name": "CloudCLI UI",
-  "short_name": "CloudCLI UI",
-  "description": "CloudCLI UI web application",
+  "name": "PocketCodex 代码代理",
+  "short_name": "PocketCodex Agent",
+  "description": "PocketCodex 代码代理工作区",
   "start_url": "/",
   "display": "standalone",
-  "background_color": "#ffffff",
-  "theme_color": "#ffffff",
+  "background_color": "#171717",
+  "theme_color": "#171717",
   "orientation": "portrait-primary",
   "scope": "/",
   "icons": [
@@ -58,4 +58,4 @@
       "purpose": "maskable any"
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/server/index.js b/server/index.js
index 61dc48c..230d409 100644
--- a/server/index.js
+++ b/server/index.js
@@ -258,7 +258,7 @@ app.use(express.static(path.join(APP_ROOT, 'dist'), {
 // System update endpoint
 app.post('/api/system/update', authenticateToken, async (req, res) => {
     if (POCKETCODEX_MODE) {
-        return res.status(403).json({ error: 'Self-update is disabled in PocketCodex mode' });
+        return res.status(403).json({ error: 'PocketCodex 模式已禁用自更新' });
     }
     try {
         // Get the project root directory (parent of server directory)
diff --git a/server/modules/database/repositories/sessions.db.ts b/server/modules/database/repositories/sessions.db.ts
index 84b7462..5be36fc 100644
--- a/server/modules/database/repositories/sessions.db.ts
+++ b/server/modules/database/repositories/sessions.db.ts
@@ -86,7 +86,7 @@ export const sessionsDb = {
     // First, ensure the project path is recorded in the projects table,
     // since it's a foreign key in the sessions table.
     if (POCKETCODEX_MODE && !projectsDb.isActiveProjectPath(normalizedProjectPath)) {
-      throw new Error('Session project is not registered or is archived');
+      throw new Error('会话项目未登记或已归档');
     }
     if (!POCKETCODEX_MODE) {
       projectsDb.createProjectPath(normalizedProjectPath);
@@ -163,7 +163,7 @@ export const sessionsDb = {
     const normalizedProjectPath = normalizeProjectPathForProvider(provider, projectPath);
 
     if (POCKETCODEX_MODE && !projectsDb.isActiveProjectPath(normalizedProjectPath)) {
-      throw new Error('Session project is not registered or is archived');
+      throw new Error('会话项目未登记或已归档');
     }
     if (!POCKETCODEX_MODE) {
       projectsDb.createProjectPath(normalizedProjectPath);
diff --git a/server/modules/projects/services/project-clone.service.ts b/server/modules/projects/services/project-clone.service.ts
index e3ad785..832647d 100644
--- a/server/modules/projects/services/project-clone.service.ts
+++ b/server/modules/projects/services/project-clone.service.ts
@@ -186,7 +186,7 @@ export async function startCloneProject(
 
   const absolutePath = pathValidation.resolvedPath;
   if (!isPocketCodexStoragePath(absolutePath, true)) {
-    throw new AppError('Clone destination must be inside the SSD or D drive workspace root', {
+    throw new AppError('克隆目标必须位于 SSD 或 D 盘工作区根目录内', {
       code: 'PROJECT_STORAGE_NOT_ALLOWED',
       statusCode: 403,
     });
diff --git a/server/modules/projects/services/project-management.service.ts b/server/modules/projects/services/project-management.service.ts
index 3da4d60..24c2373 100644
--- a/server/modules/projects/services/project-management.service.ts
+++ b/server/modules/projects/services/project-management.service.ts
@@ -108,7 +108,7 @@ export async function createProject(
 
   const resolvedProjectPath = normalizeProjectPath(pathValidation.resolvedPath);
   if (!isPocketCodexStoragePath(resolvedProjectPath)) {
-    throw new AppError('Project path must be inside the SSD or D drive workspace root', {
+    throw new AppError('项目路径必须位于 SSD 或 D 盘工作区根目录内', {
       code: 'PROJECT_STORAGE_NOT_ALLOWED',
       statusCode: 403,
     });
diff --git a/server/modules/providers/services/sessions.service.ts b/server/modules/providers/services/sessions.service.ts
index c987ba0..900f0c3 100644
--- a/server/modules/providers/services/sessions.service.ts
+++ b/server/modules/providers/services/sessions.service.ts
@@ -131,7 +131,7 @@ export const sessionsService = {
       });
     }
     if (POCKETCODEX_MODE && !projectsDb.isActiveProjectPath(normalizedProjectPath)) {
-      throw new AppError('Project is not registered or is archived.', {
+      throw new AppError('项目未登记或已归档。', {
         code: 'PROJECT_NOT_REGISTERED',
         statusCode: 400,
       });
diff --git a/server/modules/websocket/services/chat-websocket.service.ts b/server/modules/websocket/services/chat-websocket.service.ts
index 00cb097..bab9fb9 100644
--- a/server/modules/websocket/services/chat-websocket.service.ts
+++ b/server/modules/websocket/services/chat-websocket.service.ts
@@ -175,7 +175,7 @@ async function handleChatSend(
   }
 
   if (POCKETCODEX_MODE && chatRunRegistry.listRunningRuns().some((run) => run.sessionId !== sessionId)) {
-    sendProtocolError(ws, 'AGENT_BUSY', 'Another Agent task is already running.', sessionId);
+    sendProtocolError(ws, 'AGENT_BUSY', '已有另一个 Agent 任务正在运行。', sessionId);
     return;
   }
 
@@ -184,7 +184,7 @@ async function handleChatSend(
     const model = typeof clientOptions.model === 'string' ? clientOptions.model.trim() : '';
     const effort = clientOptions.effort ?? 'high';
     if (!isPocketSessionEffort(effort)) {
-      sendProtocolError(ws, 'INVALID_REASONING_EFFORT', 'Reasoning effort must be low, medium, high, or xhigh.', sessionId);
+      sendProtocolError(ws, 'INVALID_REASONING_EFFORT', '推理强度必须为 low、medium、high 或 xhigh。', sessionId);
       return;
     }
     try {
@@ -196,7 +196,7 @@ async function handleChatSend(
         await writeProviderSessionActiveModelChange(provider, { sessionId, model });
       }
     } catch {
-      sendProtocolError(ws, 'SESSION_PREFERENCES_WRITE_FAILED', 'Unable to persist session model and effort.', sessionId);
+      sendProtocolError(ws, 'SESSION_PREFERENCES_WRITE_FAILED', '无法保存会话模型和推理强度。', sessionId);
       return;
     }
   }
diff --git a/server/modules/websocket/services/shell-websocket.service.ts b/server/modules/websocket/services/shell-websocket.service.ts
index b98bb69..642cde4 100644
--- a/server/modules/websocket/services/shell-websocket.service.ts
+++ b/server/modules/websocket/services/shell-websocket.service.ts
@@ -311,7 +311,7 @@ export function handleShellConnection(
 
         const resolvedProjectPath = path.resolve(projectPath);
         if (!projectsDb.isActiveProjectPath(resolvedProjectPath)) {
-          ws.send(JSON.stringify({ type: 'error', message: 'Project is not registered or is archived' }));
+          ws.send(JSON.stringify({ type: 'error', message: '项目未登记或已归档' }));
           return;
         }
         try {
diff --git a/server/openai-codex.js b/server/openai-codex.js
index 4bfe9f7..f0c9e56 100644
--- a/server/openai-codex.js
+++ b/server/openai-codex.js
@@ -239,7 +239,7 @@ export async function queryCodex(command, options = {}, ws) {
 
   const workingDirectory = path.resolve(cwd || projectPath || process.cwd());
   if (!projectsDb.isActiveProjectPath(workingDirectory)) {
-    throw new Error('Project is not registered or is archived');
+    throw new Error('项目未登记或已归档');
   }
   const { sandboxMode, approvalPolicy } = mapPermissionModeToCodexOptions(permissionMode);
   const catalog = (await providerModelsService.getProviderModels('codex')).models;
diff --git a/src/components/app/AppContent.tsx b/src/components/app/AppContent.tsx
index 5bb15dd..61d3dae 100644
--- a/src/components/app/AppContent.tsx
+++ b/src/components/app/AppContent.tsx
@@ -213,11 +213,11 @@ function AppContentInner() {
       ) : (
         <div
-          className={`fixed inset-0 z-50 flex transition-all duration-150 ease-out ${sidebarOpen ? 'visible opacity-100' : 'invisible opacity-0'
+          className={`absolute inset-x-0 bottom-0 top-11 z-50 flex transition-all duration-150 ease-out ${sidebarOpen ? 'visible opacity-100' : 'invisible opacity-0'
             }`}
         >
           <button
-            className="fixed inset-0 bg-background/60 backdrop-blur-sm transition-opacity duration-150 ease-out"
+            className="absolute inset-0 bg-background/60 backdrop-blur-sm transition-opacity duration-150 ease-out"
             onClick={(event) => {
               event.stopPropagation();
               setSidebarOpen(false);
             }}
diff --git a/src/components/app/PocketModeBar.tsx b/src/components/app/PocketModeBar.tsx
index ca74528..b20a2cc 100644
--- a/src/components/app/PocketModeBar.tsx
+++ b/src/components/app/PocketModeBar.tsx
@@ -1,7 +1,7 @@
 const modes = [
   { label: 'GPT', href: '/?mode=gpt' },
   { label: 'Agent', href: '/agent/', active: true },
-  { label: 'Server', href: '/?mode=server' },
+  { label: '服务器', href: '/?mode=server' },
 ];
 
 export default function PocketModeBar() {
@@ -11,7 +11,7 @@ export default function PocketModeBar() {
         <span className="flex h-6 w-6 items-center justify-center rounded-md bg-primary text-xs font-bold text-primary-foreground">P</span>
         <span className="truncate text-sm font-semibold text-foreground">PocketCodex</span>
       </a>
-      <nav className="mx-auto flex h-8 items-center rounded-md border border-border bg-background p-0.5" aria-label="PocketCodex modes">
+      <nav className="mx-auto flex h-8 items-center rounded-md border border-border bg-background p-0.5" aria-label="PocketCodex 模式">
         {modes.map((mode) => (
           <a
             key={mode.label}
diff --git a/src/components/chat/hooks/useChatProviderState.ts b/src/components/chat/hooks/useChatProviderState.ts
index d0856d6..446c3d4 100644
--- a/src/components/chat/hooks/useChatProviderState.ts
+++ b/src/components/chat/hooks/useChatProviderState.ts
@@ -180,7 +180,7 @@ export function useChatProviderState({ selectedSession, selectedProject: _select
         method: 'PUT',
         body: JSON.stringify({ effort }),
       }).then((response) => {
-        if (!response.ok) throw new Error('Unable to persist session effort.');
+        if (!response.ok) throw new Error('无法保存会话推理强度。');
       }).catch((error) => console.error('Error saving session effort:', error));
       return;
     }
diff --git a/src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx b/src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx
index dd9803f..fc66a51 100644
--- a/src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx
+++ b/src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx
@@ -24,7 +24,7 @@ import {
 } from "../../../../shared/view/ui";
 
 const PROVIDER_META: { id: LLMProvider; name: string }[] = [
-  { id: "codex", name: "Codex via Sub2API" },
+  { id: "codex", name: "Codex（Sub2API）" },
 ];
 
 const MOD_KEY =
diff --git a/src/components/main-content/view/subcomponents/MainContentStateView.tsx b/src/components/main-content/view/subcomponents/MainContentStateView.tsx
index 1e8f7ed..00b6099 100644
--- a/src/components/main-content/view/subcomponents/MainContentStateView.tsx
+++ b/src/components/main-content/view/subcomponents/MainContentStateView.tsx
@@ -40,14 +40,14 @@ export default function MainContentStateView({ mode, isMobile, onMenuClick }: Ma
             <div className="mx-auto mb-5 flex h-14 w-14 items-center justify-center rounded-2xl bg-muted/50">
               <Folder className="h-7 w-7 text-muted-foreground" />
             </div>
-            <h2 className="mb-2 text-xl font-semibold text-foreground">{POCKETCODEX_MODE ? 'Choose a project' : t('mainContent.chooseProject')}</h2>
+            <h2 className="mb-2 text-xl font-semibold text-foreground">{POCKETCODEX_MODE ? '选择项目' : t('mainContent.chooseProject')}</h2>
             <p className="mb-5 text-sm leading-relaxed text-muted-foreground">
-              {POCKETCODEX_MODE ? 'Select a registered workspace to use Codex, files, Git, and terminal tools.' : t('mainContent.selectProjectDescription')}
+              {POCKETCODEX_MODE ? '选择已登记的工作区，以使用 Codex、文件、Git 和终端工具。' : t('mainContent.selectProjectDescription')}
             </p>
             <div className="rounded-xl border border-primary/10 bg-primary/5 p-3.5">
               <p className="text-sm text-primary">
                 <strong>{t('mainContent.tip')}:</strong> {POCKETCODEX_MODE
-                  ? (isMobile ? 'Open the project drawer to create an SSD or D drive workspace.' : 'Create an SSD or D drive workspace from the sidebar.')
+                  ? (isMobile ? '打开项目抽屉，可在 SSD 或 D 盘创建工作区。' : '从侧边栏可在 SSD 或 D 盘创建工作区。')
                   : (isMobile ? t('mainContent.createProjectMobile') : t('mainContent.createProjectDesktop'))}
               </p>
             </div>
diff --git a/src/components/project-creation-wizard/components/WorkspacePathField.tsx b/src/components/project-creation-wizard/components/WorkspacePathField.tsx
index 553365f..7dc6979 100644
--- a/src/components/project-creation-wizard/components/WorkspacePathField.tsx
+++ b/src/components/project-creation-wizard/components/WorkspacePathField.tsx
@@ -90,7 +90,7 @@ export default function WorkspacePathField({
   return (
     <>
       {POCKETCODEX_MODE && (
-        <div className="mb-2 flex rounded-md border border-border bg-muted/30 p-0.5" role="group" aria-label="Project storage">
+        <div className="mb-2 flex rounded-md border border-border bg-muted/30 p-0.5" role="group" aria-label="项目存储位置">
           <button
             type="button"
             onClick={() => selectStorageRoot('/workspaces/ssd')}
@@ -103,7 +103,7 @@ export default function WorkspacePathField({
             onClick={() => selectStorageRoot('/workspaces/disk')}
             className={`flex-1 rounded px-3 py-1.5 text-xs font-medium ${value.startsWith('/workspaces/disk') ? 'bg-primary text-primary-foreground' : 'text-muted-foreground hover:text-foreground'}`}
           >
-            D drive
+            D 盘
           </button>
         </div>
       )}
@@ -139,7 +139,7 @@ export default function WorkspacePathField({
           variant="outline"
           onClick={() => setShowFolderBrowser(true)}
           className="px-3"
-          title="Browse folders"
+          title="浏览文件夹"
           disabled={disabled}
         >
           <FolderOpen className="h-4 w-4" />
diff --git a/src/components/sidebar/view/subcomponents/SidebarHeader.tsx b/src/components/sidebar/view/subcomponents/SidebarHeader.tsx
index 18fd0b8..2ce7ccc 100644
--- a/src/components/sidebar/view/subcomponents/SidebarHeader.tsx
+++ b/src/components/sidebar/view/subcomponents/SidebarHeader.tsx
@@ -72,7 +72,7 @@ export default function SidebarHeader({
         className="truncate text-sm font-bold tracking-tight text-foreground"
         style={{ fontFamily: CLOUDCLI_WORDMARK_FONT_FAMILY }}
       >
-        {POCKETCODEX_MODE ? 'Agent' : t('app.title')}
+        {POCKETCODEX_MODE ? '代码代理' : t('app.title')}
       </h1>
     </div>
   );
diff --git a/src/components/sidebar/view/subcomponents/SidebarProjectList.tsx b/src/components/sidebar/view/subcomponents/SidebarProjectList.tsx
index 84b9302..0e9eb5c 100644
--- a/src/components/sidebar/view/subcomponents/SidebarProjectList.tsx
+++ b/src/components/sidebar/view/subcomponents/SidebarProjectList.tsx
@@ -4,6 +4,7 @@ import type { TFunction } from 'i18next';
 import type { LoadingProgress, Project, ProjectSession, LLMProvider } from '../../../../types/app';
 import type { SessionActivityMap } from '../../../../hooks/useSessionProtection';
 import type { MCPServerStatus, SessionWithProvider } from '../../types/types';
+import { POCKETCODEX_MODE } from '../../../../constants/config';
 
 import SidebarProjectItem from './SidebarProjectItem';
 import SidebarProjectsState from './SidebarProjectsState';
@@ -107,7 +108,7 @@ export default function SidebarProjectList({
   );
 
   useEffect(() => {
-    let baseTitle = 'CloudCLI UI';
+    let baseTitle = POCKETCODEX_MODE ? 'PocketCodex 代码代理' : 'CloudCLI UI';
     const displayName = selectedProject?.displayName?.trim();
     if (displayName) {
       baseTitle = `${displayName} - ${baseTitle}`;
diff --git a/src/components/sidebar/view/subcomponents/SidebarProjectsState.tsx b/src/components/sidebar/view/subcomponents/SidebarProjectsState.tsx
index 033d169..2117319 100644
--- a/src/components/sidebar/view/subcomponents/SidebarProjectsState.tsx
+++ b/src/components/sidebar/view/subcomponents/SidebarProjectsState.tsx
@@ -58,9 +58,11 @@ export default function SidebarProjectsState({
         <div className="mx-auto mb-4 flex h-12 w-12 items-center justify-center rounded-lg bg-muted md:mb-3">
           <Folder className="h-6 w-6 text-muted-foreground" />
         </div>
-        <h3 className="mb-2 text-base font-medium text-foreground md:mb-1">{t('projects.noProjects')}</h3>
+        <h3 className="mb-2 text-base font-medium text-foreground md:mb-1">
+          {POCKETCODEX_MODE ? '暂无项目' : t('projects.noProjects')}
+        </h3>
         <p className="text-sm text-muted-foreground">
-          {POCKETCODEX_MODE ? 'Create a registered project to start Codex.' : t('projects.runClaudeCli')}
+          {POCKETCODEX_MODE ? '创建并登记项目后即可开始使用 Codex。' : t('projects.runClaudeCli')}
         </p>
       </div>
     );
diff --git a/src/i18n/config.js b/src/i18n/config.js
index 6e9e3a9..8915f5c 100644
--- a/src/i18n/config.js
+++ b/src/i18n/config.js
@@ -13,6 +13,7 @@ import i18n from 'i18next';
 import { initReactI18next } from 'react-i18next';
 // eslint-disable-next-line import-x/order
 import LanguageDetector from 'i18next-browser-languagedetector';
+import { POCKETCODEX_MODE } from '../constants/config';
 
 // Import translation resources
 import enCommon from './locales/en/common.json';
@@ -99,14 +100,19 @@ import { languages } from './languages.js';
 // Get saved language preference from localStorage
 const getSavedLanguage = () => {
   try {
+    if (POCKETCODEX_MODE && localStorage.getItem('pocketcodex-language-initialized') !== 'true') {
+      localStorage.setItem('userLanguage', 'zh-CN');
+      localStorage.setItem('pocketcodex-language-initialized', 'true');
+      return 'zh-CN';
+    }
     const saved = localStorage.getItem('userLanguage');
     // Validate that the saved language is supported
     if (saved && languages.some(lang => lang.value === saved)) {
       return saved;
     }
-    return 'en';
+    return POCKETCODEX_MODE ? 'zh-CN' : 'en';
   } catch {
-    return 'en';
+    return POCKETCODEX_MODE ? 'zh-CN' : 'en';
   }
 };
 
diff --git a/src/utils/pageTitleNotification.ts b/src/utils/pageTitleNotification.ts
index dd786a0..00d593a 100644
--- a/src/utils/pageTitleNotification.ts
+++ b/src/utils/pageTitleNotification.ts
@@ -1,3 +1,5 @@
+import { POCKETCODEX_MODE } from '../constants/config';
+
 const COMPLETION_TITLE_INDICATOR = '[Done]';
 const TITLE_INDICATOR_CLEAR_DELAY_MS = 2000;
 
@@ -90,7 +92,7 @@ export const showCompletionTitleIndicator = (): void => {
     return;
   }
 
-  const baseTitle = stripIndicator(document.title || 'CloudCLI UI');
+  const baseTitle = stripIndicator(document.title || (POCKETCODEX_MODE ? 'PocketCodex 代码代理' : 'CloudCLI UI'));
   document.title = `${getIndicatorPrefix()}${baseTitle}`;
 
   if (pageIsActive()) {
